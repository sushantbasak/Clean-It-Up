'use strict';

const User = require('./user');
const Job = require('./job');
const Upload = require('./upload');

const schemas = {
  User,
  Job,
  Upload,
};

module.exports = schemas;
